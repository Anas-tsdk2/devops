apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - port: 3000        # port que le frontend utilise
      targetPort: 5000  # port du container backend
  type: ClusterIP